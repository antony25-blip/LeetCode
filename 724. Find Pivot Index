class Solution:
    def pivotIndex(self, nums: List[int]) -> int:
        sums=sum(nums)
        s=0
        for i in range(len(nums)):
            if (2*s)+nums[i] == sums:
                return i
            s+=nums[i]
        return -1
